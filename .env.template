# ==========================================
# CONFIGURACIÓN GLOBAL DE ARCHIVOS Y DOCKER
# ==========================================

# --- Configuración del Backend (Django) ---
DEBUG=False
SECRET_KEY=cambiar-por-un-secreto-muy-seguro-en-produccion-12345
ALLOWED_HOSTS=*

# --- Configuración de CORS ---
CORS_ALLOW_ALL_ORIGINS=True
# Si se pone CORS_ALLOW_ALL_ORIGINS en False, especificar orígenes permitidos:
# CORS_ALLOWED_ORIGINS=http://localhost

# --- Configuración de Base de Datos PostgreSQL ---
DB_NAME=bd_archivo
DB_USER=postgres
DB_PASSWORD=servidorFarmacia947
DB_HOST=db
DB_PORT=5432

# --- Configuración de Correo Electrónico ---
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=tu-correo@gmail.com
# EMAIL_HOST_PASSWORD=tu-contraseña-de-aplicacion
# DEFAULT_FROM_EMAIL=tu-correo@gmail.com

# --- Configuración de MinIO (almacenamiento de fotos) ---
# MINIO_ENDPOINT_URL=http://minio:9000
# MINIO_ROOT_USER=minioadmin
# MINIO_ROOT_PASSWORD=minioadmin
# MINIO_BUCKET_NAME=archivo-firmas
# MINIO_USE_SSL=false

# --- Configuración del Frontend ---
# En Docker con Nginx proxy, se recomienda usar '/graphql/' como URL relativa.
# En desarrollo local sin Docker, usar 'http://localhost:8000/graphql/'
VITE_GRAPHQL_URI=/graphql/
# URL base para los códigos QR de firma. Si se deja vacío, se auto-detecta.
# Útil para producción con dominio o para usar ngrok/túneles.
# Ejemplo: VITE_QR_BASE_URL=https://archivo.midominio.com
# VITE_QR_BASE_URL=
